import type { MutableRefObject } from "react";

interface EditorProps {
    content?: string;
    editorRef: MutableRefObject<any>;
}

// NHN Toast UI WYSIWYG Editor
const Editor = ({ content = "", editorRef }: EditorProps) => {
    return (
        <>
            {/*  */}
            {/*  */}
        </>
    );
};

export default Editor;
